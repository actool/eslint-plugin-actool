{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "mocha:current",
            "type": "npm",
            // FIXME: simplify (revert slashes + relativePath)
            "script": "mocha tests/lib/rules/${fileBasenameNoExtension}",
            "group": "test",
            "problemMatcher": []
        }
    ]
}
